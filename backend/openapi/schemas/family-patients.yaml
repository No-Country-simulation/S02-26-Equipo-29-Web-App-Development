FamilyPatient:
  type: object
  additionalProperties: false
  properties:
    id:
      type: string
      format: uuid

    profile_id:
      type: string
      format: uuid
      description: Profile ID del familiar

    patient_id:
      type: string
      format: uuid

    relationship:
      type: string
      description: Relación del familiar con el paciente

    created_at:
      type: string
      format: date-time

  required:
    - id
    - profile_id
    - patient_id
    - relationship
    - created_at

FamilyPatientCreateRequest:
  type: object
  additionalProperties: false
  properties:
    profile_id:
      type: string
      format: uuid
      description: Profile ID del familiar (role=family)

    relationship:
      type: string

  required:
    - profile_id
    - relationship
    
FamilyPatientUpdateRequest:
  type: object
  additionalProperties: false
  properties:
    relationship:
      type: string

